server:
    port: 9001

spring:
    application:
        name: auth
    profiles:
        active: '@profile.name@'  # dev
    cloud:
        nacos:
            discovery:
                server-addr: '@config.server-addr@'
            config:
                server-addr: '@discovery.server-addr@'
                file-extension: yaml
                #group: '@config.group@'
                
    datasource:
        url: jdbc:mysql://47.93.246.239:3306/magician?characterEncoding=utf8&useUnicode=true&useSSL=false&serverTimezone=Asia/Shanghai
        username: root
        password: redSoft!123
        type: com.alibaba.druid.pool.DruidDataSource
        druid:
            test-on-borrow: true

management:
    endpoints:
        web:
            exposure:
                include: "*"
